node_modules
.expo
dist
android
ios
src/db/migrations
bun.lock
package-lock.json
# Generated by release-please — its markdown style isn't Prettier's; don't lint it.
CHANGELOG.md
# Rewritten wholesale by release-please on every release PR (it re-serialises
# the file to set expo.version, one array element per line) and by
# `expo install` when a plugin is added. Neither runs husky, so neither can be
# formatted — and its writer disagrees with Prettier on short inline arrays.
# `expo-doctor` validates the schema in the same CI job.
app.json
