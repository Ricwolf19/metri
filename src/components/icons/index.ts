/**
 * App icons — backed by Lucide (modern, consistent, professional stroke icons).
 * Re-exported under our own names so screens import a stable `<XIcon>` and pass
 * `color` / `size` / `strokeWidth`. To add one, map another Lucide icon here.
 */
export type { LucideProps as IconProps } from 'lucide-react-native';

export {
  Home as HomeIcon,
  Activity as ActivityIcon,
  Bell as BellIcon,
  BookOpen as BookIcon,
  Settings as GearIcon,
  Camera as CameraIcon,
  ChevronRight as ChevronRightIcon,
  Flame as FlameIcon,
  LogOut as LogOutIcon,
  Shield as ShieldIcon,
} from 'lucide-react-native';
