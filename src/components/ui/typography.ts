/**
 * Cap for the OS font-size multiplier on CONTROLS (buttons, fields, chips, navbar,
 * tabs) so a fixed-width control never wraps or clips. Body copy and docs are
 * never capped. @see AGENTS.md#conventions, Text on any device
 */
export const CONTROL_FONT_SCALE = 1.3;
