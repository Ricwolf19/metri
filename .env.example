# Optional: override the backend origin (Better Auth + sync API on metri.info).
# Defaults by NODE_ENV — dev: http://10.0.2.2:3000 (Android emulator), prod: https://metri.info.
# EXPO_PUBLIC_AUTH_URL=http://localhost:3000
