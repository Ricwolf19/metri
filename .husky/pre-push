# Mirror of the CI quality job (.github/workflows/ci.yml) — verify (format +
# lint + typecheck + i18n dead keys + knip) plus the repo-wide secrets scan and
# expo-doctor. If this passes, CI passes.
bun run ci
